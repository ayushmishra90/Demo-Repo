#include<stdio.h>

int main()
{
    float a,b,sum,diff,prod,divi;
printf("enter two no.\n");
scanf("%f %f",&a,&b);
sum =a+b;
diff =a-b;
prod=a*b;
divi=a/b;

printf(" sum is %f \n",sum );
printf(" diff is %f \n",diff );
printf(" prod is %f \n",prod );
printf("divi  is %f \n",divi );
    return 0;
}